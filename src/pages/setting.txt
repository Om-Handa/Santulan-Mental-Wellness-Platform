 {activeTab === "Settings" && (
              <div className="space-y-8">
                <SectionHeader title="Settings" />
                
                <Card>
                  <SectionHeader title="Notification Preferences" />
                  <div className="space-y-4">
                    {Object.entries(preferences.notifications).map(([key, value]) => (
                      <div key={key} className="flex items-center justify-between">
                        <div>
                          <div className="font-medium">
                            {key.replace(/([A-Z])/g, ' $1').replace(/^./, str => str.toUpperCase())}
                          </div>
                          <div className="text-sm text-muted-foreground">
                            {key === 'dailyReminders' && 'Get notified about your daily quests'}
                            {key === 'questNotifications' && 'Receive updates when new quests are available'}
                            {key === 'communityUpdates' && 'Stay updated with community activities'}
                            {key === 'achievements' && 'Get notified when you earn achievements'}
                          </div>
                        </div>
                        <button className={`w-12 h-6 rounded-full transition-colors ${
                          value ? 'bg-primary' : 'bg-muted'
                        }`}>
                          <div className={`w-5 h-5 rounded-full bg-white transition-transform ${
                            value ? 'translate-x-6' : 'translate-x-0.5'
                          }`}></div>
                        </button>
                      </div>
                    ))}
                  </div>
                </Card>

                {/* Privacy Settings */}
                <div className="bg-card rounded-xl p-6 border">
                  <h3 className="text-lg font-semibold mb-4">Privacy Settings</h3>
                  <div className="space-y-4">
                    {Object.entries(preferences.privacy).map(([key, value]) => (
                      <div key={key} className="flex items-center justify-between">
                        <div>
                          <div className="font-medium">
                            {key.replace(/([A-Z])/g, ' $1').replace(/^./, str => str.toUpperCase())}
                          </div>
                          <div className="text-sm text-muted-foreground">
                            {key === 'profileVisible' && 'Allow others to view your profile'}
                            {key === 'showProgress' && 'Display your progress publicly'}
                            {key === 'allowMessages' && 'Allow community members to message you'}
                          </div>
                        </div>
                        <button className={`w-12 h-6 rounded-full transition-colors ${
                          value ? 'bg-primary' : 'bg-muted'
                        }`}>
                          <div className={`w-5 h-5 rounded-full bg-white transition-transform ${
                            value ? 'translate-x-6' : 'translate-x-0.5'
                          }`}></div>
                        </button>
                      </div>
                    ))}
                  </div>
                </div>

                {/* Account Actions */}
                <div className="bg-card rounded-xl p-6 border">
                  <h3 className="text-lg font-semibold mb-4">Account Actions</h3>
                  <div className="space-y-3">
                    <Button variant="outline" className="w-full justify-start">
                      📧 Change Email
                    </Button>
                    <Button variant="outline" className="w-full justify-start">
                      🔒 Update Password
                    </Button>
                    <Button variant="outline" className="w-full justify-start">
                      📊 Export Data
                    </Button>
                    <Button variant="outline" className="w-full justify-start text-red-600 hover:bg-red-50">
                      🗑️ Delete Account
                    </Button>
                  </div>
                </div>
              </div>
              )}

                const preferences = {
    notifications: {
      dailyReminders: true,
      questNotifications: true,
      communityUpdates: false,
      achievements: true
    },
    privacy: {
      profileVisible: true,
      showProgress: true,
      allowMessages: true
    }
  };